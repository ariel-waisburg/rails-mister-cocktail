<%= simple_form_for [ @cocktail, @dose ] do |f| %>
    <%= f.input :description %>
    <%= f.association :ingredient, include_blank: false %>
    <%= f.button :submit, class: "btn btn-success" %>
<% end %>

<%= link_to "Back to cocktail", cocktail_path(@cocktail), class: "btn btn-success" %>
